1, banner是父项目，只负责jre的管理
2, eureka_server是注册中心,只负责注册
3，tianfujiyun_clienta是一个客户端，后缀为a来区分客户端个数，
     pojo
     Map的Demo，1.8的可以学习一下；
     test中可以有1.8的LoaclDate学习；
4，tianfujiyun_clientb是一个客户端，后缀为b来区分客户端个数，
    里面有fegin的远程调用a服务的接口、及测试。
    main方法下面是跨域请求的配置。外部可直接访问。
5， tianfujiyun_clientc是客户端，后缀为c来区分客户端个数，
    里面有定时配置， redis的操作，
    在TaskTime包中全程服务@
6，tianfujiyun_cloudzuul是网管路由。所有项目皆有网管收发。统筹访问。
7，tianfujiyun_cloudzipkin暂时没有搞懂。待研究中......................


